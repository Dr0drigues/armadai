# OH1 — Lot 1 (socle event-sourcing) — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development. Steps use checkbox (`- [ ]`).

**Goal:** Poser le socle de l'orchestration event-sourcée — `ExecutionEvent`, `ExecutionState` + reducer pur `apply`, un `EventLog` (InMemory + Sqlite gated storage), et la boucle générique `run_event_sourced` (traits `Decider`/`EffectRunner`) — prouvé en isolation, sans réécrire aucun moteur.

**Architecture:** Nouveau module `src/core/orchestration/es/` (`event.rs`, `state.rs`, `log.rs`, `engine.rs`). Le log est la source de vérité ; l'état est un fold pur ; la boucle est générique sur une fonction de décision + un runner d'effet (les patterns concrets = Lots 2-4).

**Tech Stack:** Rust edition 2024, serde, rusqlite (gated storage), async_trait.

## Global Constraints

- Base = `origin/release/1.0.0`. Branche `feat/oh1-lot1-socle`, PR vers `release/1.0.0`.
- Cœur (event/state/engine) **toujours compilé** ; persistance Sqlite du log **gated `storage`** (InMemory sinon). Clippy CI 2 modes (`tui`, `tui,providers-api`) + `tui,storage` (pour le SqliteLog). fmt + test.
- **Aucun moteur réécrit dans ce lot** ; aucun chemin `run`/CLI modifié. Socle isolé, prouvé par tests.
- `apply` **pur, déterministe, total** (aucune I/O, aucun temps/hasard). Invariant testé : `state == fold(events)`.
- Spec : `docs/superpowers/specs/2026-07-21-oh1-event-sourcing-design.md`.
- Réutiliser `crate::providers::traits::ChatMessage`.

---

### Task 1: `ExecutionEvent` + `ExecutionState` + reducer `apply`

**Files:** Create `src/core/orchestration/es/mod.rs`, `es/event.rs`, `es/state.rs`; modify `src/core/orchestration/mod.rs` (`pub mod es;`).

**Interfaces produces:**
- `ExecutionEvent` enum (serde, `#[serde(tag = "t", rename_all = "snake_case")]`) — variants du spec §3 (communs + hierarchical + blackboard + ring). Chaque variant Clone/Debug.
- `ExecutionState` (Debug, Default, Clone) : champs communs `run_id: String`, `pattern: String`, `agents: Vec<String>`, `conversations: BTreeMap<String, Vec<ChatMessage>>`, `budget_tokens_in/out: u64`, `budget_cost: f64`, `status: RunStatus`, + sous-états `hier/board/ring` (structs dédiées, peuplées par leurs events).
- `enum RunStatus { Running, Completed, Halted }`.
- `pub fn apply(state: &mut ExecutionState, event: &ExecutionEvent)` — reducer pur.
- `pub fn fold(events: &[ExecutionEvent]) -> ExecutionState` (helper : `Default` + apply chaque event).

- [ ] **Step 1: Write the failing tests** (`es/state.rs` `#[cfg(test)]`)

```rust
#[cfg(test)]
mod tests {
    use super::*;
    use crate::core::orchestration::es::event::ExecutionEvent as E;

    #[test]
    fn fold_common_events_builds_state() {
        let events = vec![
            E::RunStarted { run_id: "r1".into(), pattern: "hierarchical".into(),
                agents: vec!["dev-lead".into(), "core".into()], input: "task".into(), project: None },
            E::AgentInvoked { agent: "dev-lead".into(), input: "task".into() },
            E::AgentObserved { agent: "dev-lead".into(), content: "@core: do it".into(),
                tokens_in: 10, tokens_out: 20, cost: 0.01, model: "m".into() },
            E::Completed { content: "done".into() },
        ];
        let st = fold(&events);
        assert_eq!(st.run_id, "r1");
        assert_eq!(st.pattern, "hierarchical");
        assert_eq!(st.agents.len(), 2);
        assert_eq!(st.status, RunStatus::Completed);
        assert_eq!(st.budget_tokens_in, 10);
        assert_eq!(st.budget_tokens_out, 20);
        assert!((st.budget_cost - 0.01).abs() < 1e-9);
        // conversation recorded for dev-lead (invoked + observed)
        assert!(!st.conversations.get("dev-lead").unwrap().is_empty());
    }

    #[test]
    fn fold_is_deterministic_and_equals_incremental_apply() {
        let events = vec![
            E::RunStarted { run_id: "r".into(), pattern: "blackboard".into(), agents: vec!["a".into()], input: "x".into(), project: None },
            E::AgentObserved { agent: "a".into(), content: "c".into(), tokens_in: 1, tokens_out: 2, cost: 0.0, model: "m".into() },
            E::Halted { reason: "max_rounds".into() },
        ];
        let a = fold(&events);
        let mut b = ExecutionState::default();
        for e in &events { apply(&mut b, e); }
        assert_eq!(format!("{a:?}"), format!("{b:?}"));
        assert_eq!(a.status, RunStatus::Halted);
    }

    #[test]
    fn board_and_delegate_events_update_substates() {
        let events = vec![
            E::RunStarted { run_id: "r".into(), pattern: "blackboard".into(), agents: vec!["a".into(),"b".into()], input: "x".into(), project: None },
            E::RoundStarted { round: 1 },
            E::BoardEntryAdded { agent: "a".into(), round: 1, kind: "finding".into(), content: "f".into(), refs: vec![], confidence: 0.9, tokens_in: 5, tokens_out: 5, cost: 0.0 },
        ];
        let st = fold(&events);
        assert_eq!(st.board.entries.len(), 1);
        assert_eq!(st.board.round, 1);
        assert_eq!(st.budget_tokens_in, 5);
    }
}
```

- [ ] **Step 2: Run to verify fail** — `cargo test --no-default-features --features tui -p armadai es::` → FAIL (module absent).

- [ ] **Step 3: Implement `event.rs`, `state.rs`, `mod.rs`**

`es/event.rs` — the full enum per spec §3 (common + hierarchical + blackboard + ring variants), `#[derive(Debug, Clone, Serialize, Deserialize)] #[serde(tag = "t", rename_all = "snake_case")]`. Include: `RunStarted{run_id,pattern,agents,input,project:Option<String>}`, `AgentInvoked{agent,input}`, `AgentObserved{agent,content,tokens_in:u32,tokens_out:u32,cost:f64,model:String}`, `ModelRouted{agent,tier:String,reason:String}`, `Warned{code:String}`, `Halted{reason:String}`, `Completed{content:String}`, `Delegated{from,to,task,depth:u32}`, `AskedPeer{from,to,question}`, `Escalated{from,to,message}`, `Synthesized{agent,content}`, `NestedStarted{team_lead,pattern:String}`, `NestedEnded{team_lead}`, `RoundStarted{round:u32}`, `BoardEntryAdded{agent,round:u32,kind:String,content:String,refs:Vec<usize>,confidence:f32,tokens_in:u32,tokens_out:u32,cost:f64}`, `ConsensusReached{score:f32}`, `LapStarted{lap:u32}`, `ContributionAdded{agent,lap:u32,position:usize,action:String,content:String,tokens_in:u32,tokens_out:u32,cost:f64}`, `VoteCast{agent,position:String,confidence:f32,supports:Vec<usize>,concerns:Vec<String>}`, `OutcomeResolved{outcome:String}`.

`es/state.rs` — `ExecutionState`, `RunStatus`, sub-states `HierState{trace:Vec<(String,String,String,u32)>}`, `BoardState{round:u32, entries:Vec<BoardEntryRec>}`, `RingState{lap:u32, contributions:Vec<ContribRec>, votes:BTreeMap<String,VoteRec>}` (plain reducible records, distinct from the live `Board`/`RingToken` engine types — the ES state is its own projection-friendly shape). `apply` matches every event and updates the relevant field + budget accumulators; `AgentInvoked`/`AgentObserved` push into `conversations`; `Completed`→status Completed; `Halted`→Halted. `fold(events)` helper. Provide `pub use` in `es/mod.rs`.

- [ ] **Step 4: Run tests + clippy 2 modes + fmt**

Run: `cargo test --no-default-features --features tui -p armadai es::`
Expected: PASS (3 tests).
Run: `cargo clippy --all-targets --no-default-features --features tui -- -D warnings && cargo clippy --all-targets --no-default-features --features tui,providers-api -- -D warnings && cargo fmt -- --check`
Expected: clean.

- [ ] **Step 5: Commit** `git commit -m "feat(es): ExecutionEvent + ExecutionState + pure apply reducer"`

---

### Task 2: `EventLog` (InMemory + Sqlite gated) + migration v3

**Files:** Create `src/core/orchestration/es/log.rs`; modify `es/mod.rs`; modify `src/storage/schema.rs` (migration v3).

**Interfaces:**
- Consumes: `ExecutionEvent` (Task 1).
- Produces:
  - `pub trait EventLog { fn append(&mut self, run_id: &str, event: &ExecutionEvent) -> anyhow::Result<()>; fn events(&self, run_id: &str) -> anyhow::Result<Vec<ExecutionEvent>>; }`
  - `pub struct InMemoryLog { ... }` (Map<run_id, Vec<ExecutionEvent>>).
  - `#[cfg(feature = "storage")] pub struct SqliteLog { db: crate::storage::Database }` (append → INSERT into `execution_events(run_id, seq, ts, kind, payload_json)`; events → SELECT ordered by seq, deserialize payload_json).
  - `schema.rs`: `SCHEMA_VERSION = 3`; migrate v2→v3 creates `execution_events` (PK `(run_id, seq)`, index). Base CREATE includes it (fresh DB).

- [ ] **Step 1: Write the failing tests** (`es/log.rs` — InMemory always; Sqlite gated)

```rust
#[cfg(test)]
mod tests {
    use super::*;
    use crate::core::orchestration::es::event::ExecutionEvent as E;

    fn sample() -> Vec<E> {
        vec![
            E::RunStarted { run_id: "r1".into(), pattern: "direct".into(), agents: vec!["a".into()], input: "x".into(), project: None },
            E::AgentObserved { agent: "a".into(), content: "hi".into(), tokens_in: 1, tokens_out: 1, cost: 0.0, model: "m".into() },
            E::Completed { content: "hi".into() },
        ]
    }

    #[test]
    fn in_memory_log_roundtrip_preserves_order() {
        let mut log = InMemoryLog::default();
        for e in sample() { log.append("r1", &e).unwrap(); }
        let got = log.events("r1").unwrap();
        assert_eq!(got.len(), 3);
        assert!(matches!(got[0], E::RunStarted { .. }));
        assert!(matches!(got[2], E::Completed { .. }));
        assert!(log.events("absent").unwrap().is_empty());
    }

    #[cfg(feature = "storage")]
    #[test]
    fn sqlite_log_roundtrip() {
        let db = crate::storage::init_embedded().unwrap();
        let mut log = SqliteLog::new(db);
        for e in sample() { log.append("r1", &e).unwrap(); }
        let got = log.events("r1").unwrap();
        assert_eq!(got.len(), 3);
        assert!(matches!(got[2], E::Completed { .. }));
    }
}
```

- [ ] **Step 2: Run to verify fail** — `cargo test --no-default-features --features tui,storage -p armadai es::log` → FAIL.

- [ ] **Step 3: schema v3** — in `schema.rs`: bump `SCHEMA_VERSION` to 3; add to base CREATE:
```sql
CREATE TABLE IF NOT EXISTS execution_events (
    run_id      TEXT NOT NULL,
    seq         INTEGER NOT NULL,
    ts          TEXT NOT NULL DEFAULT (datetime('now')),
    kind        TEXT NOT NULL,
    payload_json TEXT NOT NULL,
    PRIMARY KEY (run_id, seq)
);
CREATE INDEX IF NOT EXISTS idx_execution_events_run ON execution_events(run_id, seq);
```
and in `migrate`, after the v2 block: `if version < 3 { migrate_to_v3(conn)?; set user_version=3 }`. `migrate_to_v3`: `CREATE TABLE IF NOT EXISTS execution_events (...)` + index (idempotent; safe on any prior state). Tests: fresh DB = v3 with the table; a v2 DB migrates to v3 adding it.

- [ ] **Step 4: Implement `log.rs`** — `EventLog` trait + `InMemoryLog` (Default, Vec per run) + `#[cfg(feature="storage")] SqliteLog` (append computes next seq = current count for run_id; INSERT; events SELECT ordered by seq → serde_json::from_str). `kind` = the event's `t` tag (derive from serde or a small match). Register in `es/mod.rs`.

- [ ] **Step 5: Run tests + clippy (tui, tui+providers-api, tui+storage) + fmt** → PASS/clean.

- [ ] **Step 6: Commit** `git commit -m "feat(es): EventLog (InMemory + Sqlite) + execution_events schema v3"`

---

### Task 3: Boucle générique `run_event_sourced` + traits `Decider`/`EffectRunner`

**Files:** Create `src/core/orchestration/es/engine.rs`; modify `es/mod.rs`.

**Interfaces:**
- Consumes: `ExecutionState`, `apply`, `ExecutionEvent`, `EventLog` (Tasks 1-2).
- Produces:
  - `pub enum Action { Invoke { agent: String, input: String }, Emit(ExecutionEvent), Halt { reason: String }, Complete { content: String } }`.
  - `pub trait Decider { fn decide(&self, state: &ExecutionState) -> Vec<Action>; }` (déterministe, pur).
  - `#[async_trait] pub trait EffectRunner { async fn run_invoke(&self, agent: &str, input: &str, state: &ExecutionState) -> anyhow::Result<ExecutionEvent>; }` (exécute l'effet LLM → renvoie l'`AgentObserved` ; les patterns concrets fournissent l'impl en Lots 2-4).
  - `pub async fn run_event_sourced<D: Decider, R: EffectRunner, L: EventLog>(run_id: &str, initial: Vec<ExecutionEvent>, decider: &D, effects: &R, log: &mut L) -> anyhow::Result<ExecutionState>` : append+fold `initial` (dont `RunStarted`) ; boucle : `decide(state)` → pour chaque `Action` : `Invoke`→`effects.run_invoke` produit un event (+ un `AgentInvoked` avant), `Emit(e)`→e, `Halt`/`Complete`→event terminal ; chaque event est `log.append` + `apply` ; stop quand `status != Running` ou plus d'actions. Garde-fou anti-boucle (cap d'itérations configurable).
  - `pub fn replay<L: EventLog>(run_id: &str, log: &L) -> anyhow::Result<ExecutionState>` : `fold(log.events(run_id))` (aucun effet).

- [ ] **Step 1: Write the failing test** (`engine.rs` — mock decider + effect prove the loop + replay)

```rust
#[cfg(test)]
mod tests {
    use super::*;
    use crate::core::orchestration::es::event::ExecutionEvent as E;
    use crate::core::orchestration::es::log::InMemoryLog;
    use async_trait::async_trait;

    // Decider: invoke "a" once (when no observation yet), then complete.
    struct D;
    impl Decider for D {
        fn decide(&self, s: &ExecutionState) -> Vec<Action> {
            let observed = s.conversations.get("a").map(|c| !c.is_empty()).unwrap_or(false);
            if !observed { vec![Action::Invoke { agent: "a".into(), input: "go".into() }] }
            else { vec![Action::Complete { content: "final".into() }] }
        }
    }
    struct Eff;
    #[async_trait]
    impl EffectRunner for Eff {
        async fn run_invoke(&self, agent: &str, _input: &str, _s: &ExecutionState) -> anyhow::Result<E> {
            Ok(E::AgentObserved { agent: agent.into(), content: "resp".into(), tokens_in: 3, tokens_out: 4, cost: 0.0, model: "m".into() })
        }
    }

    #[tokio::test]
    async fn loop_runs_appends_folds_and_terminates() {
        let mut log = InMemoryLog::default();
        let init = vec![E::RunStarted { run_id: "r".into(), pattern: "direct".into(), agents: vec!["a".into()], input: "go".into(), project: None }];
        let st = run_event_sourced("r", init, &D, &Eff, &mut log).await.unwrap();
        assert_eq!(st.status, RunStatus::Completed);
        assert_eq!(st.budget_tokens_in, 3);
        // replay from the log reconstructs the same state, no effects re-run
        let replayed = replay("r", &log).unwrap();
        assert_eq!(format!("{st:?}"), format!("{replayed:?}"));
        // log contains RunStarted, AgentInvoked, AgentObserved, Completed
        assert!(log.events("r").unwrap().len() >= 4);
    }
}
```

- [ ] **Step 2: Run to verify fail** — `cargo test --no-default-features --features tui,providers-api -p armadai es::engine` → FAIL.

- [ ] **Step 3: Implement `engine.rs`** — `Action`, `Decider`, `EffectRunner`, `run_event_sourced` (append+fold initial ; loop with iteration cap e.g. 500 ; `Invoke` → append `AgentInvoked` then the observed event from `run_invoke` ; `Emit`/`Halt`/`Complete`), `replay`. Helper to append+apply in one step.

- [ ] **Step 4: Run tests + clippy 2 modes + fmt** → PASS/clean.

- [ ] **Step 5: Commit** `git commit -m "feat(es): generic run_event_sourced loop + Decider/EffectRunner + replay"`

---

## Notes pour l'implémenteur
- **Socle isolé** : ne toucher NI aux moteurs existants (hierarchical/blackboard/ring/direct), NI au chemin `run`/CLI, NI aux projections. Juste le nouveau module `es/` + la migration v3 (additive). Les ~850 tests existants doivent rester verts.
- `apply` **pur/total/déterministe** — aucune I/O, `Instant::now`/`Date` interdits dans apply (les timestamps vivent dans le log/Sqlite `ts`, pas dans l'état).
- Les sous-états ES (`BoardState`/`RingState`/`HierState`) sont des **records réductibles propres**, distincts des types moteur vivants (`Board`/`RingToken`) — ne pas les coupler (les Lots 2-4 mapperont).
- `EffectRunner` async (appel LLM) ; `Decider`/`apply` sync purs. La boucle est le seul async.
- La migration v3 réutilise le mécanisme `user_version` (C9 Lot 2) : additive, idempotente, testée fresh + v2→v3.
