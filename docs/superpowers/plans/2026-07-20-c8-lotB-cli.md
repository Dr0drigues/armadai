# C8 Lot B — CLI (flags de routage + câblage + dry-run) — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development. Steps use checkbox (`- [ ]`).

**Goal:** Exposer le routage d'agents en CLI (`--route`, `--tags`, `--dry-run`), câbler la sélection dans `run_orchestrated`, avec garde-fou « ≥2 agents pour blackboard/ring » et warning pour hierarchical.

**Architecture:** Un helper pur `apply_agent_selection` (filtre/réordonne le roster chargé via `select_agents` du Lot A) + les flags clap threadés `execute → run_inner → run_orchestrated`. Tout opère sur le **roster chargé** : une route est un sous-ensemble nommé du roster (agent de route absent du roster → erreur claire).

**Tech Stack:** Rust edition 2024, clap.

## Global Constraints

- Base = `origin/release/1.0.0` (@ `e8b9094`, après C8 Lot A). Branche `feat/c8-lotB-cli`, PR vers `release/1.0.0`.
- Clippy 2 modes CI `-D warnings` : `--no-default-features --features tui` ET `--features tui,providers-api`. `cargo fmt -- --check`. `cargo test`.
- **Rétro-compat** : sans `--route`/`--tags`/`--dry-run`, comportement **identique** (roster complet, exécution normale).
- Réutiliser `agent_selection::{select_agents, AgentSelection, SelectionError}` (Lot A) et `RunEvent::AgentSelect` (Lot A). Retirer le `#[allow(dead_code)]` sur `AgentSelect` (il est maintenant construit).
- Spec : `docs/superpowers/specs/2026-07-20-c8-agent-routing-design.md`.

---

### Task 1: Helper `apply_agent_selection` (filtre/réordonne le roster) + tests

**Files:**
- Modify: `src/cli/run.rs` (helper + tests)

**Interfaces:**
- Consumes : `crate::core::orchestration::agent_selection::{select_agents, AgentSelection}`, `crate::core::agent::Agent`, `crate::providers::traits::Provider`.
- Produces :
  ```rust
  fn apply_agent_selection(
      agents: Vec<Agent>,
      providers: Vec<std::sync::Arc<dyn Provider>>,
      route: Option<&str>,
      tags: &[String],
      routes: &std::collections::BTreeMap<String, Vec<String>>,
  ) -> anyhow::Result<(Vec<Agent>, Vec<std::sync::Arc<dyn Provider>>, AgentSelection)>
  ```
  Filtre et **réordonne** les paires (agent, provider) selon `select_agents`. Un nom retenu absent du roster chargé → erreur claire.

- [ ] **Step 1: Write the failing tests**

Dans le module `#[cfg(test)]` de `run.rs` (créer s'il n'existe pas), ajouter un provider factice minimal et les tests :

```rust
#[cfg(test)]
mod selection_tests {
    use super::*;
    use std::collections::BTreeMap;
    use std::sync::Arc;
    use crate::core::agent::{Agent, AgentMetadata};
    use crate::providers::traits::{
        ChatMessage, CompletionRequest, CompletionResponse, Provider, ProviderMetadata, TokenStream,
    };
    use async_trait::async_trait;
    use std::path::PathBuf;

    struct DummyProvider(String);
    #[async_trait]
    impl Provider for DummyProvider {
        async fn complete(&self, _r: CompletionRequest) -> anyhow::Result<CompletionResponse> {
            anyhow::bail!("not used")
        }
        async fn stream(&self, _r: CompletionRequest) -> anyhow::Result<TokenStream> {
            anyhow::bail!("not used")
        }
        fn metadata(&self) -> ProviderMetadata {
            ProviderMetadata { name: self.0.clone(), models: vec![], supports_streaming: false }
        }
    }

    fn agent_with_tags(name: &str, tags: &[&str], stacks: &[&str]) -> Agent {
        Agent {
            name: name.to_string(),
            source: PathBuf::from(format!("{name}.md")),
            metadata: AgentMetadata {
                provider: "mock".to_string(),
                model: Some("mock".to_string()),
                command: None, args: None, temperature: 0.7, max_tokens: None, timeout: None,
                tags: tags.iter().map(|s| s.to_string()).collect(),
                stacks: stacks.iter().map(|s| s.to_string()).collect(),
                scope: vec![], model_fallback: vec![], cost_limit: None, rate_limit: None,
                context_window: None, mode: None, orchestration: None, triggers: None, ring_config: None,
            },
            system_prompt: "p".to_string(), instructions: None, output_format: None,
            pipeline: None, context: None,
        }
    }

    fn roster() -> (Vec<Agent>, Vec<Arc<dyn Provider>>) {
        let agents = vec![
            agent_with_tags("sec", &["security"], &["rust"]),
            agent_with_tags("ui", &["frontend"], &[]),
            agent_with_tags("qa", &["testing"], &[]),
        ];
        let providers: Vec<Arc<dyn Provider>> = agents
            .iter()
            .map(|a| Arc::new(DummyProvider(a.name.clone())) as Arc<dyn Provider>)
            .collect();
        (agents, providers)
    }

    #[test]
    fn no_selectors_keeps_full_roster_in_order() {
        let (a, p) = roster();
        let (agents, providers, sel) =
            apply_agent_selection(a, p, None, &[], &BTreeMap::new()).unwrap();
        assert_eq!(agents.iter().map(|x| x.name.clone()).collect::<Vec<_>>(), vec!["sec","ui","qa"]);
        assert_eq!(providers.len(), 3);
        assert_eq!(sel.agents, vec!["sec","ui","qa"]);
    }

    #[test]
    fn tags_filter_and_align_providers() {
        let (a, p) = roster();
        let (agents, providers, _sel) =
            apply_agent_selection(a, p, None, &["security".to_string()], &BTreeMap::new()).unwrap();
        assert_eq!(agents.iter().map(|x| x.name.clone()).collect::<Vec<_>>(), vec!["sec"]);
        assert_eq!(providers.len(), 1);
        assert_eq!(providers[0].metadata().name, "sec"); // provider realigned to the kept agent
    }

    #[test]
    fn route_selects_named_subset_reordered() {
        let (a, p) = roster();
        let mut routes = BTreeMap::new();
        routes.insert("r".to_string(), vec!["qa".to_string(), "sec".to_string()]);
        let (agents, providers, _sel) =
            apply_agent_selection(a, p, Some("r"), &[], &routes).unwrap();
        // Order follows the route, not the roster.
        assert_eq!(agents.iter().map(|x| x.name.clone()).collect::<Vec<_>>(), vec!["qa","sec"]);
        assert_eq!(providers[0].metadata().name, "qa");
        assert_eq!(providers[1].metadata().name, "sec");
    }

    #[test]
    fn route_referencing_absent_agent_errors() {
        let (a, p) = roster();
        let mut routes = BTreeMap::new();
        routes.insert("r".to_string(), vec!["sec".to_string(), "ghost".to_string()]);
        let err = apply_agent_selection(a, p, Some("r"), &[], &routes).unwrap_err();
        assert!(err.to_string().contains("ghost"), "error should name the missing agent: {err}");
    }

    #[test]
    fn unknown_route_propagates_error() {
        let (a, p) = roster();
        let err = apply_agent_selection(a, p, Some("nope"), &[], &BTreeMap::new()).unwrap_err();
        assert!(err.to_string().to_lowercase().contains("route"));
    }
}
```

- [ ] **Step 2: Run to verify fail**

Run: `cargo test --no-default-features --features tui,providers-api -p armadai selection_tests`
Expected: FAIL (`apply_agent_selection` undefined).

- [ ] **Step 3: Implement `apply_agent_selection`**

Dans `run.rs` (près de `run_orchestrated`), ajouter :

```rust
/// Apply C8 agent selection (routes/tags) to a loaded roster, returning the
/// filtered and reordered (agents, providers) plus the selection metadata.
/// Everything operates on the loaded roster: a route naming an agent absent
/// from the roster is a clear error (the agent must be provided to the run).
fn apply_agent_selection(
    agents: Vec<crate::core::agent::Agent>,
    providers: Vec<std::sync::Arc<dyn crate::providers::traits::Provider>>,
    route: Option<&str>,
    tags: &[String],
    routes: &std::collections::BTreeMap<String, Vec<String>>,
) -> anyhow::Result<(
    Vec<crate::core::agent::Agent>,
    Vec<std::sync::Arc<dyn crate::providers::traits::Provider>>,
    crate::core::orchestration::agent_selection::AgentSelection,
)> {
    use std::collections::HashMap;

    let roster: Vec<String> = agents.iter().map(|a| a.name.clone()).collect();
    let mut agent_tags: HashMap<String, Vec<String>> = HashMap::new();
    for a in &agents {
        let mut t = a.metadata.tags.clone();
        t.extend(a.metadata.stacks.iter().cloned());
        agent_tags.insert(a.name.clone(), t);
    }

    let selection = crate::core::orchestration::agent_selection::select_agents(
        &roster, route, tags, routes, &agent_tags,
    )
    .map_err(|e| anyhow::anyhow!("{e}"))?;

    // Index the loaded pairs by name, then rebuild in selection order.
    let mut by_name: HashMap<String, (crate::core::agent::Agent, std::sync::Arc<dyn crate::providers::traits::Provider>)> =
        HashMap::new();
    for (a, p) in agents.into_iter().zip(providers) {
        by_name.insert(a.name.clone(), (a, p));
    }

    let mut out_agents = Vec::with_capacity(selection.agents.len());
    let mut out_providers = Vec::with_capacity(selection.agents.len());
    for name in &selection.agents {
        let (a, p) = by_name.remove(name).ok_or_else(|| {
            anyhow::anyhow!(
                "route/selection references agent '{name}' which is not among the run's agents \
                 (add it via --pipe or the orchestration config)"
            )
        })?;
        out_agents.push(a);
        out_providers.push(p);
    }

    Ok((out_agents, out_providers, selection))
}
```

- [ ] **Step 4: Run to verify pass**

Run: `cargo test --no-default-features --features tui,providers-api -p armadai selection_tests`
Expected: PASS (5 tests).

- [ ] **Step 5: Clippy 2 modes + fmt**

Run: `cargo clippy --all-targets --no-default-features --features tui -- -D warnings && cargo clippy --all-targets --no-default-features --features tui,providers-api -- -D warnings && cargo fmt -- --check`
Expected: clean.

- [ ] **Step 6: Commit**

```bash
git add src/cli/run.rs
git commit -m "feat(cli): apply_agent_selection helper (filter/reorder roster by route/tags)"
```

---

### Task 2: Flags clap + câblage + `--dry-run` + garde-fous

**Files:**
- Modify: `src/cli/mod.rs` (flags + dispatch)
- Modify: `src/cli/run.rs` (`execute`, `run_inner`, `run_orchestrated` signatures + wiring)
- Modify: `src/core/events.rs` (retirer `#[allow(dead_code)]` sur `AgentSelect`)

**Interfaces:**
- Consumes : `apply_agent_selection` (Task 1), `RunEvent::AgentSelect`.
- Produces : flags `--route <name>`, `--tags <csv>`, `--dry-run` sur `run`, threadés jusqu'à `run_orchestrated`.

- [ ] **Step 1: Add the clap flags**

Dans `src/cli/mod.rs`, variant `Command::Run` (après `max_content`, ≈ ligne 81), ajouter :

```rust
        /// C8: select agents by a named route from `orchestration.routes`
        #[arg(long, value_name = "NAME")]
        route: Option<String>,
        /// C8: select agents whose tags/stacks intersect these (comma-separated)
        #[arg(long, value_name = "TAGS", value_delimiter = ',')]
        tags: Option<Vec<String>>,
        /// C8: resolve and print the agent selection without executing (0 tokens)
        #[arg(long)]
        dry_run: bool,
```

Dans le dispatch (`Command::Run { .. } =>`, ≈ ligne 447), déstructurer et passer les nouveaux champs à `run::execute(...)`.

- [ ] **Step 2: Thread through `execute` and `run_inner`**

Dans `run.rs`, ajouter les paramètres `route: Option<String>`, `tags: Option<Vec<String>>`, `dry_run: bool` à `execute` (et au bloc `#[allow(clippy::too_many_arguments)]`), et les passer à `run_inner`. Ajouter les mêmes à `run_inner` (déjà `#[allow(clippy::too_many_arguments)]`). Normaliser : `let tags = tags.unwrap_or_default();`.

Passer `route.as_deref()`, `&tags`, `dry_run` aux deux appels de `run_orchestrated` (ligne ≈120 explicite, et ≈141 config-hierarchical).

- [ ] **Step 3: Extend `run_orchestrated` signature + wire selection**

Ajouter à `run_orchestrated` les paramètres `route: Option<&str>`, `tags: &[String]`, `dry_run: bool`.

Après la boucle qui construit `agents: Vec<Agent>` + `providers` (juste avant `match pattern { ... }`), insérer :

```rust
    // ── C8: deterministic agent selection (routes/tags) ────────
    let routing_active = route.is_some() || !tags.is_empty();
    if routing_active && pattern == "hierarchical" {
        sink.emit(&RunEvent::Warning {
            code: "routing_ignored_hierarchical".to_string(),
            from: None,
            to: None,
        });
    } else if routing_active {
        let routes = match resolution {
            AgentResolution::Project { config, .. } => config
                .orchestration
                .as_ref()
                .map(|o| o.routes.clone())
                .unwrap_or_default(),
            _ => std::collections::BTreeMap::new(),
        };
        let (sel_agents, sel_providers, selection) =
            apply_agent_selection(agents, providers, route, tags, &routes)?;
        agents = sel_agents;
        providers = sel_providers;

        sink.emit(&RunEvent::AgentSelect {
            selected: selection.agents.clone(),
            reason: selection.reason.clone(),
        });

        // blackboard/ring need >= 2 agents.
        if (pattern == "blackboard" || pattern == "ring") && agents.len() < 2 {
            anyhow::bail!(
                "agent routing selected {} agent(s); pattern '{pattern}' requires >= 2 \
                 (selection: {})",
                agents.len(),
                selection.reason
            );
        }

        if dry_run {
            eprintln!(
                "[dry-run] pattern '{pattern}' — {} ({} agent(s)): {}",
                selection.reason,
                agents.len(),
                agents.iter().map(|a| a.name.clone()).collect::<Vec<_>>().join(", ")
            );
            if !json {
                println!(
                    "{}",
                    agents.iter().map(|a| a.name.clone()).collect::<Vec<_>>().join("\n")
                );
            }
            return Ok(());
        }
    }
```

Note : rendre `agents` et `providers` mutables (`let mut agents`, `let mut providers`) à leur déclaration. Si `routing_active` est faux OU hierarchical, `agents`/`providers` restent le roster complet (comportement actuel).

Une subtilité `agent_names` : les blocs `blackboard`/`ring`/`hierarchical` construisent des events `RunStart`/`emit_agent_ends`/`Result` à partir de `agent_names` (les noms d'origine). Après sélection, utiliser les noms **sélectionnés** pour ces events. Remplacer les usages de `agent_names` **après la sélection** par la liste dérivée de `agents` (ex. `let agent_names: Vec<String> = agents.iter().map(|a| a.name.clone()).collect();` recalculé juste après le bloc de sélection, en shadowing du paramètre). Vérifier que `RunStart` (émis AVANT la boucle de chargement, ≈ ligne 562) reste sur le roster initial — c'est acceptable (le run démarre sur le roster demandé), mais `emit_agent_ends`/`Result` doivent refléter la sélection. Recalculer le `agent_names` local après sélection couvre ces usages en aval.

- [ ] **Step 4: Remove the now-unnecessary `#[allow(dead_code)]`**

Dans `src/core/events.rs`, retirer le `#[allow(dead_code)]` sur le variant `AgentSelect` (il est désormais construit dans `run_orchestrated`). Vérifier que clippy reste clean.

- [ ] **Step 5: Build + regression + clippy**

Run: `cargo build --no-default-features --features tui,providers-api`
Expected: compiles.
Run: `cargo test --no-default-features --features tui,providers-api -p armadai`
Expected: PASS (selection_tests + no regression).
Run: `cargo clippy --all-targets --no-default-features --features tui -- -D warnings && cargo clippy --all-targets --no-default-features --features tui,providers-api -- -D warnings && cargo fmt -- --check`
Expected: clean.

- [ ] **Step 6: Commit**

```bash
git add src/cli/mod.rs src/cli/run.rs src/core/events.rs
git commit -m "feat(cli): --route/--tags/--dry-run agent routing wired into orchestration"
```

---

## Notes pour l'implémenteur

- **Validation manuelle** : `armadai run --orchestrate blackboard --pipe a b c --tags security --dry-run "task"` doit afficher la sélection + « [dry-run] » sans exécuter ; sans `--dry-run`, exécuter sur le sous-ensemble. `armadai run --orchestrate blackboard --pipe a --route unknown "t"` → erreur route inconnue. Décrire ces étapes dans le rapport.
- **Modèle borné (v1)** : une route sélectionne un sous-ensemble du **roster chargé** (`--pipe`). Une route citant un agent absent du roster → erreur explicite (documenté ; charger des agents hors roster serait une évolution future).
- `--tags` utilise `value_delimiter = ','` → `--tags security,rust` donne `["security","rust"]`.
- Ne PAS toucher au moteur d'orchestration (Lot A l'a fait) ni au chargement des agents ; la sélection s'applique après le build, sur les paires (agent, provider).
- Follow-ups de couverture hérités du Lot A (facultatifs) : test du chemin route+tags `NoMatch`, assertion sur la chaîne `reason`, dédup des agents dupliqués dans une route.
